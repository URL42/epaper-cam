.DS_Store
build/

# native harness binaries and their scratch output
dither/test_dither
dither/tune
*.pbm
*.pgm

# Photographs never go in this repo. The reference captures in frames/ are of
# a real home and a real face, and they are only needed locally for tuning the
# tone curve with dither/tune. Keep them on disk, out of git.
frames/
*.png
*.jpg
*.jpeg
*.rtf

# Generated image data for PaperCam_ImageTest: ~1MB of source, derived from a
# personal photo, and regenerated in one command by dither/tune --emit-header.
PaperCam_ImageTest/image_data.h

# Manufacturer datasheets: every page is footered "SEEKINK Confidential".
# Kept locally; the facts we rely on are cited in CLAUDE.md instead.
*.pdf
